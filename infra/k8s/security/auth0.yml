apiVersion: security.istio.io/v1beta1
kind: RequestAuthentication
metadata:
  name: "auth0"
  namespace: cf
spec:
  jwtRules:
    - issuer: "https://baturalpk.eu.auth0.com/"
      jwksUri: "https://baturalpk.eu.auth0.com/.well-known/jwks.json"
